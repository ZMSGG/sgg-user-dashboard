# MY SGG Home Hero — Image 2.0 Exploration v001

Status: `CANDIDATE / PRIVATE OWNER REVIEW`

## Goal

Create ten genuinely new home-hero options using the canonical SEVENGODS Games
GODS–OTOMO pairs. These are selection candidates only. Nothing in this folder
is approved for runtime, `public/`, deployment, social publishing, or external
distribution.

## Current slot

- Existing Home image slot: `app/Dashboard.tsx` hero only.
- Working canvas: landscape 16:9, targeting a final `1600 × 900` hero.
- Desktop copy zone: keep `x=0–58%` dark and low-detail.
- Shared desktop/mobile identity-safe zone: `x=45–72%, y=12–68%`.
- Preferred face zone: `x=62–69%, y=18–45%`.
- Mobile copy zone: keep `y=58–100%` dark and low-detail.
- Avoid critical details in the top-right status-widget area.

## Character and rights rules

- `character_usage`: `BOTH`
- Show exactly one canonical GODS–OTOMO pair per candidate.
- Preserve canonical pair IDs and the explicitly selected OTOMO form.
- Use only the owner-approved Creator Kit v1 WebP files as identity references.
- References are AI input only; never copy, crop, trace, re-encode, or publish
  them as the candidate.
- Each output must be a substantive Image 2.0 reconstruction with a new scene,
  pose, composition, lighting, and dashboard-specific safe area.
- No baked text, names, numerals, logos, UI, watermarks, or signatures.
- Keep all candidates family-friendly and non-sexualized.

## Review gate

Every generated image requires actual-image checks for identity, pair, OTOMO
form, reference similarity, anatomy, desktop crop, mobile crop, embedded text,
and watermark. Only an owner-selected candidate may later move to an approval
record and a new Image 2.0 final production pass.

